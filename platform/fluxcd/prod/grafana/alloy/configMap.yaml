apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-config
  namespace: grafana
data:
  values.yaml: |
    alloy:
      configMap:
        create: false
        name: alloy-config
        key: config.alloy
  config.alloy: |
    logging {
      level = "info"
    }
